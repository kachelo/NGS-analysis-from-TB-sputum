< config.mk
# Load Global Variables

# Transform BAM into SAM format
#
results/%.sam:     data/%.bam
	set -x
	mkdir -p `dirname $target`
	samtools view -h $prereq > $target'.build' \
	&& mv $target'.build' $target

# Clean up generated files
#
clean:V:
	bin/targets | xargs rm -f
